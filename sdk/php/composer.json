{
  "name": "ttd/capture",
  "description": "PHP capture SDK for Traceweave: zero-edit, non-blocking, comprehensive per-function capture via a file:// stream wrapper that instruments source at load time.",
  "type": "library",
  "require": {
    "php": ">=8.1",
    "nikic/php-parser": "^5.0"
  },
  "autoload": {
    "psr-4": {
      "TtdCapture\\": "src/"
    }
  }
}
